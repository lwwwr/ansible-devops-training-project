version: "3"
services:
  jenkins:
    build: .
    volumes:
      - /home/{{ user_name }}/{{ work_dir }}/jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - "8080:8080"
      - "50000:50000"
    restart: unless-stopped
