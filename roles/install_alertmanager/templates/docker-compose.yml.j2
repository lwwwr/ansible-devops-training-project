version: "3"
services:
  alertmanager:
    image: prom/alertmanager
    volumes:
      - /home/{{ user_name }}/{{ work_dir }}/alertmanager_home:/etc/alertmanager/
    ports:
      - "9093:9093"
    restart: unless-stopped
